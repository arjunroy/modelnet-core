/* Ethan Eade, 8/30/02
 * This header defines utility functions for general use.
 */
#ifndef __UTIL_H_
#define __UTIL_H_

#include <mngraph.h>
#include <set>
#include <list>
using namespace std;

//Define MIN and MAX macros

#define MIN(A,B) ((A)<(B)?(A):(B))
#define MAX(A,B) ((A)>(B)?(A):(B))

// findNodes takes a graph and adds all nodes matching a 
// given type to the results list.
int findNodes(MNGraph& g, string type, list<vertex>& results);

//randInt return a random integer 0..2^31-1
int randInt();


#endif
